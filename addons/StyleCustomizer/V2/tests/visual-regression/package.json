{
  "name": "visual-regression",
  "version": "1.0.0",
  "description": "The single highest-leverage safety net for the v2 work: it proves that a form re-rendered through the new CSS-variable pipeline (or after a legacy→v2 migration) looks **pixel-identical** to before. Build the baseline **before** the frontend refactor / migration; nothing merges until this stays green.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pixelmatch": "^7.2.0",
    "playwright": "^1.61.1",
    "pngjs": "^7.0.0"
  }
}
